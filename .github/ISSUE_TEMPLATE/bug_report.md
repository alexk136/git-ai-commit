---
name: Bug report
about: Something doesn't work
title: "[bug] "
labels: bug
---

## What happened

<!-- One sentence summary of the bug. -->

## Reproduction

```bash
# Exact commands you ran. Include the working directory and any
# environment setup. If the bug is environment-specific, mention
# your OS, bash version (`bash --version`), and git version.
```

## Expected

<!-- What you expected to happen. -->

## Actual

<!-- What actually happened. Include the full output of the tool,
especially the error message. Use a code block so it stays readable. -->

```
<paste full output here>
```

## Environment

- **Provider**: `ollama` / `openai` / `openrouter` / `anthropic` / `minimax`
- **Model** (if you set one):
- **Bash version** (`bash --version`):
- **OS**:
- **git-ai-commit version** (`git-ai-commit --help` shows it; if not, run `git log -1 --oneline`):

## Logs

<!-- If you can, run with `--verbose` and paste the output. -->
